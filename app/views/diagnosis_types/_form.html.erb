<%= form_with(model: diagnosis_type) do |form| %>
  <% if diagnosis_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(diagnosis_type.errors.count, "error") %> prohibited this diagnosis_type from being saved:</h2>

      <ul>
        <% diagnosis_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :idn %>
    <%= form.number_field :idn %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :description_en %>
    <%= form.text_field :description_en %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
