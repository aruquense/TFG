<%= form_with(model: diagnosis_ci) do |form| %>
  <% if diagnosis_ci.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(diagnosis_ci.errors.count, "error") %> prohibited this diagnosis_ci from being saved:</h2>

      <ul>
        <% diagnosis_ci.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :idn %>
    <%= form.number_field :idn %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :description_EN %>
    <%= form.text_field :description_EN %>
  </div>

  <div class="field">
    <%= form.label :erase %>
    <%= form.number_field :erase %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
